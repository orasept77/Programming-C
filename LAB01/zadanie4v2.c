#include <stdio.h>

int main() // kalkulator program
{
    double var1, wynik;

    printf("parzystomierz");
    printf("\nPodaj liczbe: ");
    scanf("%lf", &var1);
    while (var1 / 2)
    {
        printf("ta liczba jest podzielna jest 2", wynik); break;
    }

    return 0;
}